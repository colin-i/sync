Description: <short summary of the patch>
 TODO: Put a short summary on the line above and replace this paragraph
 with a longer explanation of this change. Complete the meta-information
 with other relevant fields (see below for details). To make it easier, the
 information below has been extracted from the changelog. Adjust it or drop
 it.
 .
 actionswf (1-125) bionic; urgency=medium
 .
   * jpeg/png/gif
Author: bc <bc@bc-desktop>

---
The information above should follow the Patch Tagging Guidelines, please
checkout https://dep.debian.net/deps/dep3/ to learn about the format. Here
are templates for supplementary fields that you might want to add:

Origin: (upstream|backport|vendor|other), (<patch-url>|commit:<commit-id>)
Bug: <upstream-bugtracker-url>
Bug-Debian: https://bugs.debian.org/<bugnumber>
Bug-Ubuntu: https://launchpad.net/bugs/<bugnumber>
Forwarded: (no|not-needed|<patch-forwarded-url>)
Applied-Upstream: <version>, (<commit-url>|commit:<commid-id>)
Reviewed-By: <name and email of someone who approved/reviewed the patch>
Last-Update: 2025-02-27

--- actionswf-1.orig/.github/workflows/deb.yml
+++ actionswf-1/.github/workflows/deb.yml
@@ -33,7 +33,7 @@ jobs:
       cd in
       debuild -S -us -uc
    - name: unsigned debs
-     uses: actions/upload-artifact@v3
+     uses: actions/upload-artifact@v4
      with:
       name: sdebs
       path: |
--- actionswf-1.orig/.github/workflows/rpm.yml
+++ actionswf-1/.github/workflows/rpm.yml
@@ -17,7 +17,7 @@ jobs:
    - name: Run a multi-line script
      run: |
       #get deb
-      arh=`cat debian/control | grep ^Architecture | cut -d ' ' -f2`
+      arh=`cat debian/control | grep ^Architecture | head -1 | cut -d ' ' -f2`
       name=`cat debian/changelog | head -1 | grep -o ^[^\ ]*`
       version=`cat debian/changelog | head -1 | grep -Poe "[^\(]*(?=\))"`
       #dist=`cat debian/changelog | head -1 | grep -Poe "[^ ]*(?=;)"`
@@ -37,7 +37,7 @@ jobs:
       mv ${fname} ../${fname}
       echo "file=${fname}" >> $GITHUB_ENV
    - name: artifact
-     uses: actions/upload-artifact@v3
+     uses: actions/upload-artifact@v4
      with:
       name: rpm
       path: ./${{ env.file }}
--- actionswf-1.orig/dev/actionswf.h
+++ actionswf-1/dev/actionswf.h
@@ -126,6 +126,17 @@ int swf_dbl_width(char* imagepath);
 //height
 int swf_dbl_height(char* imagepath);
 
+//jpeg/png/gif
+
+//id
+int swf_jpeg(char* imagepath);
+//id
+int swf_jpeg_alpha(char* imagepath,char* alphapath);
+//width
+int swf_gif_width(char* imagepath);
+//height
+int swf_gif_height(char* imagepath);
+
 //back at generic swf functions
 
 void swf_done();
--- actionswf-1.orig/dev/importf.oh
+++ actionswf-1/dev/importf.oh
@@ -36,6 +36,14 @@ importX "swf_dbl_ex" swf_dbl_ex
 importX "swf_dbl_width" swf_dbl_width
 #height
 importX "swf_dbl_height" swf_dbl_height
+#id
+importX "swf_jpeg" swf_jpeg
+#id
+importX "swf_jpeg_alpha" swf_jpeg_alpha
+#width
+importX "swf_gif_width" swf_gif_width
+#height
+importX "swf_gif_height" swf_gif_height
 #utils
 importX "swf_done" swf_done
 importX "swf_new" swf_new
--- actionswf-1.orig/include/prog.h
+++ actionswf-1/include/prog.h
@@ -66,7 +66,9 @@ const DefineShape3=32
         const solid_fill=0
         const repeating_bitmap_fill=0x40
 const DefineBitsLossless=20
+const DefineBitsJPEG2=21
 const DefineButton2=34
+const DefineBitsJPEG3=35
 const DefineBitsLossless2=36
 const DefineEditText=37
 const DefineSprite=39
@@ -142,3 +144,7 @@ const doubleH_sign=0x80000000
 const R_OK=4
 
 include "xlog.oh"
+
+const dblsignature=4
+const dblsize=4
+const dblformat=1
--- actionswf-1.orig/src/file.oc
+++ actionswf-1/src/file.oc
@@ -61,13 +61,20 @@ importx "free" free
 import "error" error
 
 #file
+function f_open(sd path,sd format)
+	sd f;setcall f fopen(path,format)
+	if f=(NULL)
+		vstr err="fopen error"
+		call error(err)
+	end
+	return f
+end
+#file
 function f_open_mem(sd path,sd format)
 	sd f;setcall f fopen(path,format)
 	if f=(NULL)
 		call free(path)
-		char a="fopen error"
-		vstr e^a
-		call error(e)
+		call error(f_open.err)
 	endif
 	call free(path)
 	return f
@@ -189,3 +196,23 @@ function f_tell_errors(sd f)
 	call fclose(f)
 	call error(f_tell.er)
 endfunction
+
+import "file_resources_set" file_resources_set
+import "file_resources_free" file_resources_free
+function filepath_get_word(ss imagepath,sd offset,ss err)
+	sd file
+	setcall file file_open(imagepath,(_open_read))
+	call file_resources_set(file)
+	sd size
+	setcall size filesize(file)
+	sd offsetandword;set offsetandword offset
+	add offsetandword (WORD)
+	if size<^offsetandword
+		call error(err)
+	endif
+	call file_seek(file,offset,(SEEK_SET))
+	sw dim=0  #sw is long at the moment
+	call file_read(file,#dim,(WORD))
+	call file_resources_free()
+	return dim
+endfunction
--- actionswf-1.orig/src/local.oc
+++ actionswf-1/src/local.oc
@@ -456,6 +456,7 @@ function swf_image_ex_local(ss imagepath
     setcall shape_id swf_shape_bitmap_local(dbl_id,width,p_wh#)
     return shape_id
 endfunction
+import "swf_tag_recordheader_long_entry" swf_tag_recordheader_long_entry
 #id
 function swf_dbl_ex_local(ss imagepath,sd p_wh)
 #ss imagepath = path name for the dbl image
@@ -465,7 +466,7 @@ function swf_dbl_ex_local(ss imagepath,s
     sd size
     sd cursor
     setcall mem file_get_content(imagepath,#size)
-    if size<8
+    if size<(dblsignature+dblsize)
         call error("missing image header")
     endif
     char hd_magic1={D,B,l,1};vdata magic1^hd_magic1
@@ -491,7 +492,6 @@ function swf_dbl_ex_local(ss imagepath,s
     endif
     #add the define bits lossless file to mem
     add size (WORD)
-    import "swf_tag_recordheader_long_entry" swf_tag_recordheader_long_entry
     call swf_tag_recordheader_long_entry(header,size)
     setcall id identifiers_get()
     call swf_mem_add(#id,(WORD))
@@ -513,6 +513,51 @@ function swf_dbl_ex_local(ss imagepath,s
     return id
 endfunction
 
+import "f_open" f_open
+importx "fseek" fseek
+importx "fclose" fclose
+import "f_tell_errors" f_tell_errors
+#id
+function swf_jpeg_local(ss imagepath,sd tag,ss alphapath)
+#ss imagepath = path name for the image
+#sd tag = jpeg 2 or 3
+#ss alphapath = path name for the image alpha
+	sd mem
+	sd size
+	setcall mem file_get_content(imagepath,#size)
+	sd allsize;set allsize size
+	if tag=(DefineBitsJPEG3)
+		sd file;set file f_open(alphapath,"rb")
+		call fseek(file,0,(SEEK_END))
+		sd size2;set size2 f_tell_errors(file)
+		call fclose(file) #i m closing it because i don't want to hardcode error(), but that means to add another little check here, in the meantime, another prog can fast modify everything and here no held at resources
+		add allsize size2
+		if allsize<0 #going to sign bit
+			call error("two big files")
+		end
+		add allsize (DWORD)
+	end
+	add allsize (WORD) #if going to sign bit, the error will be catched at realloc
+	call swf_tag_recordheader_long_entry(tag,allsize)
+	sd id
+	setcall id identifiers_get()
+	call swf_mem_add(#id,(WORD))
+	if tag=(DefineBitsJPEG3)
+		call swf_mem_add(#size,(DWORD))
+	end
+	call swf_mem_add(mem,size)
+	call file_get_content__resources_free()
+	if tag=(DefineBitsJPEG3)
+		setcall mem file_get_content(alphapath,#size)
+		if size!=size2
+			call error("sizes hazard at jpeg 3")
+		end
+		call swf_mem_add(mem,size)
+		call file_get_content__resources_free()
+	end
+	return id
+endfunction
+
 function swf_placeobject_local(sd refid,sd depth)
 #sd refid    the id
 #sd depth    depth value
--- actionswf-1.orig/src/swf.e.oc
+++ actionswf-1/src/swf.e.oc
@@ -14,8 +14,6 @@ import "button_mem" button_mem
 
 import "stack_to_word_arg" stack_to_word_arg
 import "word_arg_to_stack" word_arg_to_stack
-import "file_resources_set" file_resources_set
-import "file_resources_free" file_resources_free
 import "struct_ids" struct_ids
 import "block_get_size" block_get_size
 import "block_get_mem" block_get_mem
@@ -291,7 +289,7 @@ functionX swf_dbl(ss imagepath)
 base swf_new.bp;call ebase(swf_new.bp)
 #ss imagepath = path name for the dbl image
     sd id
-    setcall id swf_dbl_ex_local(imagepath,0)
+    setcall id swf_dbl_ex_local(imagepath,(NULL))
     return id
 endfunction
 #id
@@ -301,46 +299,63 @@ functionX swf_dbl_ex(ss imagepath,sd p_w
 	sd id;set id swf_dbl_ex_local(imagepath,p_wh)
 	return id
 endfunction
-import "file_open" file_open
-import "file_seek" file_seek
-import "file_read" file_read
-import "filesize" filesize
+import "filepath_get_word" filepath_get_word
 
 #width
 functionX swf_dbl_width(ss imagepath)
 base swf_new.bp;call ebase(swf_new.bp)
 #ss imagepath = path name for the dbl image
-    sd file
-    setcall file file_open(imagepath,(_open_read))
-    call file_resources_set(file)
-    sd size
-    setcall size filesize(file)
-    if size<(4+4+1+2)
-        call error("invalid dbl file")
-    endif
-    call file_seek(file,(4+4+1),(SEEK_SET))
-    sd width=0
-    call file_read(file,#width,2)
-    call file_resources_free()
-    return width
+	base swf_new.bp;call ebase(swf_new.bp)
+	#format? ming newSWFDBLBitmap_fromInput
+	vstru err="invalid dbl file"
+	sw dim;set dim filepath_get_word(imagepath,(dblsignature+dblsize+dblformat),err)
+	return dim
 endfunction
 #height
 functionX swf_dbl_height(ss imagepath)
 base swf_new.bp;call ebase(swf_new.bp)
 #ss imagepath = path name for the dbl image
-    sd file
-    setcall file file_open(imagepath,(_open_read))
-    call file_resources_set(file)
-    sd size
-    setcall size filesize(file)
-    if size<(4+4+1+2+2)
-        call error("invalid dbl file")
-    endif
-    call file_seek(file,(4+4+1+2),(SEEK_SET))
-    sd height=0
-    call file_read(file,#height,2)
-    call file_resources_free()
-    return height
+	base swf_new.bp;call ebase(swf_new.bp)
+	sw dim;set dim filepath_get_word(imagepath,(dblsignature+dblsize+dblformat+WORD),swf_dbl_width.err)
+	return dim
+endfunction
+
+import "swf_jpeg_local" swf_jpeg_local
+#https://stackoverflow.com/questions/7676701/java-jpeg-converter-for-odd-image-types
+#https://www.w3.org/Graphics/JPEG/jfif3.pdf
+#FFD8, can skip FFE[0-F] bigendianSize(usually 0010) = skip 20 bytes, FFDB...FFD9
+#
+#https://www.w3.org/Graphics/GIF/spec-gif89a.txt
+#Transparent Color Flag
+#anyway, more simple is: identify -format %A a.gif
+functionX swf_jpeg(ss imagepath)
+#ss imagepath = path name for the dbl image
+	sd id
+	setcall id swf_jpeg_local(imagepath,(DefineBitsJPEG2))
+	return id
+endfunction
+functionX swf_jpeg_alpha(ss imagepath,ss alphapath)
+#ss imagepath = path name for the dbl image
+	sd id
+	setcall id swf_jpeg_local(imagepath,(DefineBitsJPEG3),alphapath)
+	return id
+endfunction
+
+const gifsignature=6 #GIF89a
+#width
+functionX swf_gif_width(ss imagepath)
+#ss imagepath = path name for the gif image
+	base swf_new.bp;call ebase(swf_new.bp)
+	vstr err="invalid gif file"
+	sw dim;set dim filepath_get_word(imagepath,(gifsignature),err)
+	return dim
+endfunction
+#height
+functionX swf_gif_height(ss imagepath)
+#ss imagepath = path name for the gif image
+	base swf_new.bp;call ebase(swf_new.bp)
+	sw dim;set dim filepath_get_word(imagepath,(gifsignature+WORD),swf_gif_width.err)
+	return dim
 endfunction
 
 ##############
