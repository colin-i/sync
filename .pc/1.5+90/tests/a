
if [ -z "${OCOMP}" ]; then OCOMP=../src/o; fi
if [ -z "${OLINK}" ]; then OLINK=../ounused/ounused; fi

logs=
objs=
for f in "${@}"; do
	${OCOMP} ${f}.s || exit 1
	logs+=" ${f}.s.log"
	objs+=" ${f}.o"
done
${OLINK} ${logs} && \
cc ${objs} && \
./a.out
echo $?
